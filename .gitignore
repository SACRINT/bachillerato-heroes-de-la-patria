# Node.js
node_modules/
npm-debug.log
yarn-debug.log
yarn-error.log
.pnpm-store/
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel/
.vercelignore # This file itself should be committed, but its contents define what Vercel ignores.

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lcov-report/
coverage/

# IDEs and editors
.vscode/
.idea/
.DS_Store
*.sublime-project
*.sublime-workspace

# Build artifacts
dist/
build/
out/

# Backups and temporary files
backups/
*.bak
*.tmp
*.swp
*~.nib
*.swo
temp-test-content.txt
cookies.txt

# Diagnostic reports
diagnostic-report.json
build-report.json
audit-report.json
backend/test-report-direct.json

# Agent-specific files
.claude/
.genkit/
MEMORIA_AGENTE.md # This is my own memory, should not be committed.
CLAUDE.md # Claude's memory, should not be committed.
GEMINI.md # My own memory, should not be committed.
*.md
!README.md # Mantener README.md en GitHub para referencia del repositorio
*.txt
# Unused/Legacy files (as per user's list and context)
.gitignore2
no_usados/
nul

# Backend specific ignores
backend/node_modules/
backend/.env
backend/backups/
backend/server.log

# Server specific ignores (if 'server' is a separate Node.js project)
server/node_modules/
server/.env

# Other specific files from the list that should be ignored
firebase-debug.log
test_real_login.json

EJECUTAR_EN_NEON.sql
SCRIPT_VERIFICACION_DASHBOARD.js
actualizacion/
public/SCRIPT_VERIFICACION_DASHBOARD.js
setup-security.bat