# Archivos del sistema
.DS_Store
Thumbs.db
nul 

# Logs y Reportes de Herramientas
coverage/
audit-report.json
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archivos temporales
*.tmp
*.temp

# Node modules (todas las instancias)
node_modules/

# Archivos de Entorno y Dependencias
.env
.env.local
.env.database
.env.example # El template de variables de entorno

# Archivos de editor
.vscode/
.idea/
*.swp
*.swo

# Archivos de cache
.cache/

# ============================================
# 🧹 LIMPIEZA OCTUBRE 2025 (Reglas existentes)
# ============================================

# Directorio de archivos no usados (COMPLETO)
no_usados/
03_BachilleratoHeroesWeb_copia/

# Documentación local (no subir a GitHub ni a Vercel para despliegue)
# Generalizado para ignorar todos los archivos Markdown excepto README.md
# *.md
!README.md # Mantener README.md en GitHub para referencia del repositorio
!CHANGELOG.md
!MASTER-CHECKLIST-BGE-2025.md
!CLAUDE.md
# !docs/*.md # Permitir todos los archivos .md dentro de docs/

# Reportes de texto (ya cubierto por *.log, pero se mantiene si hay otros .txt)
# *.txt

# Backups
*.backup
*.backup_*

# Archivos temporales de Office
~$*.md
~$*.docx

# Scripts temporales de desarrollo
fix-*.sh
test-*.sh
update-*.py
SCRIPT_*.js

# Otros Subdirectorios
actualizacion/

# Build reports
build-report.json
public/build-report.json

# Archivos de configuración temporal (revisar si config/ es realmente temporal)
# Si la carpeta config/ en la raíz o backend/config/ son esenciales para la app,
# esta regla es demasiado amplia. Asumo que se refiere a archivos temporales
# dentro de config/, no a la carpeta completa.
# Mantengo la regla original, pero con esta nota.
# config/

# Package lock (mantener package-lock.json es una buena práctica para builds reproducibles)
# La línea original estaba comentada, la elimino para asegurar que se incluya.
# package-lock.json # NO IGNORAR - ESENCIAL PARA BUILDS REPRODUCIBLES

# Backend específico
backend/nul
backend/backups/
backend/data/sessions.json
backend/data/users.json
backend/test-*.js
backend/test-report-*.json
backend/data/ # Ignorar toda la carpeta de datos del backend

# Public duplicados (mantenemos solo raíz)
# Nota: public/ se mantiene para Vercel static serving

# Carpetas de Datos y Contenido Generado
# public/uploads/
backups/
backup/

# Archivos de Configuración Sensibles
# Carpetas que podrían contener claves de API o configuraciones locales.
# public/config/
.genkit/
.vercel/
# .claude/
.claude/agents
# GitHub Actions (si no se usa)
# .github/

# API lock files temporales (revisar si api/package-lock.json es realmente temporal)
# package-lock.json debe ser versionado para sub-proyectos también.
# Si es un archivo temporal, debe ser más específico.
# Por ahora, lo mantengo como estaba en el original.
# api/package-lock.json

# Vercel local config/cache (ya cubierto por .vercel/)
# .vercel

# Archivos Obsoletos y de Prueba
# Archivos de experimentación que ya no son parte de la aplicación final.
force-admin.html
*-tenant-*.html
sistema-multi-tenant-final.html
aviso-privacidad-elegante.html
ux-demo.html

# ============================================
# 🤖 RECOMENDACIONES (OCTUBRE 2025)
# ============================================

# Scripts de Desarrollo y Herramientas Locales (adiciones/generalizaciones)
scripts/ # Ignorar toda la carpeta scripts/ en la raíz
backend/scripts/ # Ignorar toda la carpeta scripts/ en backend/
audit-unused-files.js
move-unused-files.js
restore-critical-fase4-files.js
restore-development-files.js
restore-future-vision-files.js
setup-security.bat
test-forms.js # Ya cubierto por *.js si se ignora scripts/, pero explícito
test_real_login.json

# Archivos de Base de Datos (scripts de ejecución única, no parte del runtime)
EJECUTAR_EN_NEON.sql

# Directorios de Build
dist/
# public/dist/
backend/node_modules/
# NOTA: backend/package-lock.json DEBE estar en GitHub para que npm ci funcione en Vercel
# NO ignorar este archivo - es esencial para reproducibilidad en CI/CD