# 🔒 VARIABLES DE ENTORNO - BGE HÉROES DE LA PATRIA
# ================================================
# IMPORTANTE: Copiar este archivo como .env y configurar valores reales

# ⚡ CRÍTICAS (OBLIGATORIAS)
# ========================
# JWT Secret para firmar tokens (64 caracteres aleatorios)
JWT_SECRET=GENERAR_RANDOM_64_CARACTERES_AQUI

# Session Secret para express-session (64 caracteres aleatorios)
SESSION_SECRET=GENERAR_RANDOM_64_CARACTERES_AQUI

# Usuario administrador
ADMIN_USERNAME=admin

# Hash bcrypt de la contraseña admin (12 rounds)
# Generar con: node -e "console.log(require('bcrypt').hashSync('TU_PASSWORD_AQUI', 12))"
ADMIN_PASSWORD_HASH=HASH_BCRYPT_12_ROUNDS_AQUI

# Entorno de ejecución
NODE_ENV=production

# 🌐 IMPORTANTES (RECOMENDADAS)
# ============================
# Origen permitido para CORS
CORS_ORIGIN=https://heroesdelapatria.edu.mx

# URL de conexión a base de datos
DATABASE_URL=postgresql://usuario:password@localhost:5432/bge_heroes

# Rate limiting - ventana de tiempo en milisegundos (15 min)
RATE_LIMIT_WINDOW_MS=900000

# Rate limiting - máximo de requests por ventana
RATE_LIMIT_MAX_REQUESTS=100

# 🔧 OPCIONALES (CONFIGURABLES)
# =============================
# Puerto del servidor
PORT=3000

# HSTS Max Age en segundos (1 año)
SECURITY_HSTS_MAX_AGE=31536000

# URI para reportes CSP
CSP_REPORT_URI=/api/csp-report

# 📝 NOTAS DE CONFIGURACIÓN
# ========================
# 1. Generar JWT_SECRET y SESSION_SECRET aleatorios:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# 2. Generar ADMIN_PASSWORD_HASH:
#    node -e "console.log(require('bcrypt').hashSync('TU_PASSWORD_SEGURO', 12))"
#
# 3. En producción, usar variables de entorno del servidor
# 4. Nunca commitear el archivo .env real
# 5. Verificar que todas las variables críticas estén configuradas