// Make sure functions are globally accessible
        if (typeof toggleChatbot !== 'undefined') window.toggleChatbot = toggleChatbot;
        if (typeof sendMessage !== 'undefined') window.sendMessage = sendMessage;
        // handleKeyPress is now defined globally in script.js
